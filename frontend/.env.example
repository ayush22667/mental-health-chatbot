# Backend API URL
VITE_API_URL=http://localhost:3000

# AWS Cognito Configuration (Optional - for production)
# Leave empty to use backend JWT authentication
# Get these values from: cdk deploy outputs or AWS Console
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_AWS_REGION=ap-south-1
